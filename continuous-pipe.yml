tasks:
    images:
        build:
            services:
                app:
                    image: docker.io/inviqasession/cp-website

    deploy:
        deploy:
            cluster: fra-01
            services:
                app:
                    specification:
                        accessibility:
                            from_external: true

filter: code_reference.branch != 'gh-pages'
