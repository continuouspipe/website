tasks:
    - build:
          services:
              app:
                  image: docker.io/inviqasession/cp-website

    - deploy:
          cluster: fra-01

filter: code_reference.branch != 'gh-pages'
